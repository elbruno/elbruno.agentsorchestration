Open-Meteo is a free weather API that doesn't require an API key. Let me create the console app using it.Open-Meteo is a free weather API that doesn't require an API key. Let me create the console app using it.Build succeeded. The console app is ready:

**Files created:**
- `WeatherApp.csproj` - .NET 10 console project
- `Program.cs` - Main app using Open-Meteo free API (no API key needed)

**Features:**
- Fetches real-time temperature for Toronto, Tokyo, and Madrid via Open-Meteo API
- Displays weather emoji: ☀️ (≥25°C warm), 🌤️ (≥10°C mild), ❄️ (<10°C cold)
- Clean formatted output with error handlingBuild succeeded. The console app is ready:

**Files created:**
- `WeatherApp.csproj` - .NET 10 console project
- `Program.cs` - Main app using Open-Meteo free API (no API key needed)

**Features:**
- Fetches real-time temperature for Toronto, Tokyo, and Madrid via Open-Meteo API
- Displays weather emoji: ☀️ (≥25°C warm), 🌤️ (≥10°C mild), ❄️ (<10°C cold)
- Clean formatted output with error handling