Created three data models:

1. **WeatherData.cs** - Main display model with `CityName`, `TemperatureCelsius`, `Description`, and computed `WeatherEmoji` (☀️ ≥20°C, 🌤️ 10-19°C, ❄️ <10°C)

2. **OpenMeteoResponse.cs** - API response model for [Open-Meteo](https://open-meteo.com/) (free, no API key required)

3. **CityCoordinates.cs** - Coordinates for Toronto, Tokyo, and Madrid with a static `TargetCities` arrayCreated three data models:

1. **WeatherData.cs** - Main display model with `CityName`, `TemperatureCelsius`, `Description`, and computed `WeatherEmoji` (☀️ ≥20°C, 🌤️ 10-19°C, ❄️ <10°C)

2. **OpenMeteoResponse.cs** - API response model for [Open-Meteo](https://open-meteo.com/) (free, no API key required)

3. **CityCoordinates.cs** - Coordinates for Toronto, Tokyo, and Madrid with a static `TargetCities` array